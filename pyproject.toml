[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-metrics"
version = "0.1.0"
description = "Fleet tile analytics — domain distribution, confidence, growth rate"
license = {text = "MIT"}
requires-python = ">=3.8"
